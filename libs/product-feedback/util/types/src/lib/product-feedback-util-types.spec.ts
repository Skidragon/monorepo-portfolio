import { productFeedbackUtilTypes } from './product-feedback-util-types';

describe('productFeedbackUtilTypes', () => {
  it('should work', () => {
    expect(productFeedbackUtilTypes()).toEqual('product-feedback-util-types');
  });
});
