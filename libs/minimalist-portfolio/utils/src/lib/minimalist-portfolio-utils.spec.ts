import { minimalistPortfolioUtils } from './minimalist-portfolio-utils';

describe('minimalistPortfolioUtils', () => {
  it('should work', () => {
    expect(minimalistPortfolioUtils()).toEqual('minimalist-portfolio-utils');
  });
});
